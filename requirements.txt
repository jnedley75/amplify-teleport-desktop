# Original repo deps (keep these unchanged unless testing shows breakage)
aiortc-datachannel-only==1.3.2.post3
requests==2.28.2
cryptography<39

# New deps for system tray, icon handling, Windows integration, and bundling
pystray>=0.19.5          # Core for creating/managing the Windows system tray icon + menu
pillow>=10.0.0           # Required by pystray for loading/processing the tray icon image (PIL.Image)
pywin32>=306             # For Windows-specific features: admin elevation checks, registry (auto-start), notifications, etc.
pyinstaller>=6.0         # To bundle everything into a single .exe (no Python install required for end users)
win10toast>=0.9          # For better Windows toast notifications on connect/disconnect/errors